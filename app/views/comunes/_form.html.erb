<%= form_with(model: comune, local: true) do |form| %>
  <% if comune.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comune.errors.count, "error") %> prohibited this comune from being saved:</h2>

      <ul>
      <% comune.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :region_id %>
    <%= form.text_field :region_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
