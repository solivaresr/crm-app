<%= form_with(model: sale_executive, local: true) do |form| %>
  <% if sale_executive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sale_executive.errors.count, "error") %> prohibited this sale_executive from being saved:</h2>

      <ul>
      <% sale_executive.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :password %>
    <%= form.text_field :password %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
